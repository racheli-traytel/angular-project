import { DCourseDirective } from './d-course.directive';

describe('DCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new DCourseDirective();
    expect(directive).toBeTruthy();
  });
});
